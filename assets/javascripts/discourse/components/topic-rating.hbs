<div class="topic-rating">
  {{#each this.stars as |star|}}
    <span 
      class="star {{if star.filled "filled"}}"
      role="button"
      {{on "click" (fn this.setRating star.value)}}>
      {{if star.filled "★" "☆"}}
    </span>
  {{/each}}
</div>
